# Project Status (living doc)

**Every agent and human: read this first, and update it before you push.**
The `pre-push` hook and CI block code changes that don't touch this file. (Docs-only pushes are fine.)

## How to update (takes 1 minute)
1. Edit **your module's row** in the table below: status, what's next, blockers.
2. Add **one line** at the top of *Recent changes*: `YYYY-MM-DD — module — what changed and why it matters to others`.
3. If you changed behavior, thresholds, a protocol, or made a decision → update the matching `docs/spec/0X-*.md` too. Decisions also go in the Decisions log in `docs/spec/00-overview.md`.
4. Contracts (`frontend/src/lib/contracts.ts`, `backend/schemas.py`, `docs/spec/01-architecture.md`) always change **together**, and announce it in team chat.
**Owners are fluid** (`open` = unclaimed): put your name/handle in the Owner column when you start a row (several people can share one), clear it when you stop, and push the claim immediately. Pull before you claim.
Status values: `not started` · `in progress` · `blocked` · `done (untested live)` · `done`.

## Module board
| Module | Owner | Status | Notes / next / blockers |
|---|---|---|---|
| Foundation (repo, contracts, state machine, risk fn, API skeleton) | — | done | Vite+React+TS frontend, FastAPI backend, shared contracts, tests green |
| Alerts (Twilio SMS only, demo-number guard, dry-run) | majesticcoder14 | done (untested live) | Dry-run succeeds and the demo destination is configured locally. **Blocked on Twilio setup: get a Twilio number + verify the demo phone (docs/SMS-SETUP.md, `python scripts/sms_check.py`).** Twilio SID, auth token, and sender number are still missing; next: add them to `.env`, verify the destination in Twilio, then send one consented SMS with `DRY_RUN=false`. |
| Frontend / full UI + UX | leo + majesticcoder14 | done (untested live) | **Whole storyboard built**: home + consent panel, 4 test screens, info document, 3-band result, countdown, dashboard, demo panel. Vision failures now retry once automatically and then expose **Try again**, so a bad frame or pre-smile cannot strand the screen. Open: live camera pass, second-opinion consent checkbox (text ready in `lib/privacy/consentText.ts`, no capture UI yet); consent + Clear my data done (spec 06 "Privacy") |
| Voice agent (ElevenLabs) | open | done (untested live) | Signed-URL endpoint, `useAgent`, transcript callbacks, and client tools are implemented. Next: configure the agent/API key and verify a live conversation end to end. |
| Vision: framing gates | majesticcoder14 | done (untested live) | `checkFaceFraming` / `checkArmFraming` + tests, wired to live landmarks in the runtime, plus a yaw hint ("Look straight at the screen") |
| Vision: face + arm metrics | majesticcoder14 | done (untested live) | `analyzeFace` / `analyzeArms` implemented + synthetic-fixture tests; thresholds UNCALIBRATED, left/right mapping ASSUMED. **Do not reimplement.** Open: verify left/right on a real camera (`?debug=1`), then calibrate on teammate recordings (spec 02) |
| Vision: MediaPipe runtime (`useMediaPipe`, capture controller, test runner) | majesticcoder14 | done (untested live) | Webcam → landmarks → capture timing → test runner. Invalid captures retry once after showing the reason for 2 s, then expose **Try again**; bad framing times out at 12 s. Models committed in `frontend/public/models/`; wasm copied on `pnpm install`. Open: run the `?debug=1` live checks (GETTING-STARTED §8) |
| Validation tooling (held-out split, freeze, criteria; `pnpm tune/freeze/validate`, `python -m models.validate`) | majesticcoder14 | done (synthetic only) | See docs/VALIDATION.md. Deterministic per-person tune/validate split, threshold freeze fingerprint, PASS/FAIL/INSUFFICIENT criteria with Wilson bounds, ramp suggestions (never auto-applied), PUBLIC reports. TS and Python tools cross-checked (splits, Wilson, percentile, AUC identical). **Open: record people (≥ 8 validation people for meaningful bounds), tune, freeze, validate** |
| Calibration tooling (`?record=1` recorder, `pnpm calibrate` replay) | majesticcoder14 | in progress | Face, arms and eyes now record exact analyzer inputs with labelled scenarios; replay prints false alarms / misses / wrong sides. **Next: live hardware pass, then everyone records ~26 vision runs (see `docs/CALIBRATION.md`) and tunes thresholds** |
| Speech: core analysis (DSP features, scoring, QC) | majesticcoder14 | in progress | First clean real-mic run exposed a false positive (severity 0.67): rate/prosody ramps were too strict and running-speech HNR was invalid. Corrected with an anonymous metric regression; **next: repeat healthy + mimicked runs across people**. Thresholds remain uncalibrated. |
| Speech: browser recorder + panels (`lib/speech/*`) | open | done (untested live) | 16 kHz WAV capture, auto-stop, `speechRunner.runSpeech()`, isolated `?record=speech` scenarios. Never run on a real mic: see the manual checklist in the recorder report / GETTING-STARTED |
| Speech: endpoint + calibration CLI | majesticcoder14 | done | `/api/speech/analyze` hardened (5 MB, 10 s, retry-not-500); `python -m models.calibrate` |
| Speech: ElevenLabs Scribe transcript + voice agent | open | done (untested live) | Agent client tools call the real speech and vision runners; verify agent mic muting and live transcript behavior with configured ElevenLabs credentials. |
| Eyes test (BE-FAST) — now in the MVP | majesticcoder14 + leo | done (untested live), **wired + flag ON** | `createEyesCapture` + `runEyes` + `EyeTest` screen now drive `analyzeEyes`/`EyeStimulus`; `FEATURES.eyesTest = true` and the order is Eyes → Face → Arms → Speech. Open: agent tool `start_eye_test`, **live verify of the left/right mapping (`?debug=1`)**, calibrate. Flip the flag off if it misbehaves |
| Phoneme scoring (wav2vec2 via PyTorch, optional) | majesticcoder14 | done (TTS-verified only) | `models/phoneme.py`; `pip install -r requirements-ml.txt` + `python -m models.phoneme --download` (378 MB); enable with `PHONEME_SCORING=true`; warmed at backend startup. Real-speech behavior unverified |
| Stretch: Gemini vision second opinion | majesticcoder14 | backend done (not run against the real API) | `models/vision.py` calls Gemini (free tier) with validation + unclear-on-any-failure, 19 offline tests. **Off by default: needs `SECOND_OPINION=true` AND a `GEMINI_API_KEY` in `.env` (aistudio.google.com/apikey; free tier may train on / human-review images) and a live check. Frontend capture of the 2 JPEGs + consent checkbox + dashboard label is NOT wired.** |
| Deploy (Vercel + Railway) | backend | not started | Backend runs on demo laptop; Railway is backup (torch-free) |

## Open decisions / blockers
- Verify demo phone number in Twilio; confirm the trial number can text it.
- ElevenLabs agent credentials still need to be configured and verified live.
- Backup demo teammate for the patient role: TBD.
- **Decision needed:** speech max risk weight is 0.5, so a clear speech-only deficit (0.45) does NOT alert while a clear face/arm deficit does. Raise `MAX_WEIGHTS.speech` to 0.6 for "any one FAST sign alerts"? Revisit once speech is calibrated (spec 05, `consistency.test.ts`).
- **Speech risks to watch:** noisy rooms/accents/cheap mics raise severity (10 dB SNR TTS scored 0.32) and could false-alarm; phoneme model has no declared licence (credit it in the pitch); it needs ~1.4 GB RAM on the demo laptop; real-mic capture (AudioWorklet, 16 kHz, echo-cancellation off) is untested.
- Live-camera verification of left/right mapping (face landmarks, blendshapes, arms 11/13/15, eye landmarks) is pending on real hardware: see spec 02 "Shared conventions".
- Test order is Eyes → Face → Arms → Speech (patient steps back once, then returns close). Change `testSequence()` in `frontend/src/lib/config.ts` if the team disagrees.

## Known issues
- All thresholds/weights are uncalibrated (see spec files). Calibrate on teammate fixtures around hour ~20.

## Recent changes (newest first)
- 2026-09-19 — ui/docs/agent — **honest-claims pass**: one shared disclaimer (`frontend/src/lib/disclaimer.ts` + `components/ui/Disclaimer.tsx`: only a BE-FAST guide, not clinically accurate, not a medical device, cannot diagnose or rule out a stroke) on home, consent list, every result band, info page, test screens and a footer; removed "Check for a stroke", "Low risk detected", green low band, "errs toward telling you to get help", "screens", "Cancel — I am OK", "second opinion" and the reassuring agent line; info-page stats labelled as published research; `docs/agent-prompt.md` has a new "Always be honest about what this is" section and a non-reassuring nothing-flagged line. **The live ElevenLabs agent prompt still needs the same change (lead applies).** New `lib/disclaimer.test.ts` (banned phrases + usage). Spec 00/04/06/07, README, VALIDATION updated.
- 2026-09-19 — vision/speech/privacy — **Gemini live-verified**: `gemini-2.5-flash` is closed to new keys (HTTP 404), default is now `gemini-3.6-flash` (~1.6-2.5 s with thinking off; `thinkingBudget` is sent only to models that accept it, `gemini-3.5-flash-lite` rejects it with 400). Real call with a synthetic image returned `unclear` ("no face visible"). The second opinion stays OFF (`SECOND_OPINION`) and the key lives only in the local `.env`. **Phoneme scoring turned on locally** (`PHONEME_SCORING=true` in `.env`; `.env.example` default stays off): model cached, ~0.7 s/clip warm, metrics (`per`, `gop_*`) appear in real `/api/speech/analyze` output; a synthetic formant clip scores 0.36, so verify on a real mic before trusting it. Added `scripts/elevenlabs_privacy.py` (preview by default, `--apply` needs the team's OK) for the ElevenLabs retention/recording settings in PRIVACY.md section 6; **not applied yet**.
- 2026-09-19 — privacy — merged the three privacy audits (PRIVACY.md data map + vendor terms, backend hardening, frontend consent/clear-data/CSP). Corrected two user-facing claims found on review: the speech clip is NOT sent to ElevenLabs (no Scribe call exists), and the voice guide copy now says ElevenLabs may keep the recording and transcript (the live agent has `record_voice: true`, `retention_days: -1`). **Open, needs a human: ElevenLabs dashboard privacy settings (docs/PRIVACY.md §6), a deletion-request contact, legal review of the law summaries, CSP live test, self-host the libsamplerate worklet, swap the Railway wildcard in `vercel.json` for the real backend origin.**
- 2026-09-19 — docs/privacy — added `docs/PRIVACY.md`: code-derived data map, verified ElevenLabs/Twilio/Gemini terms, honest compliance posture (no HIPAA claim), recommended copy, and a gap list. **Live ElevenLabs agent keeps audio + transcripts indefinitely (`retention_days -1`, `record_voice true`); FAQ and consent copy do not disclose the voice-guide mic stream.** Docs only; no code or contracts touched.
- 2026-09-19 — backend/privacy — **hardening pass**: Gemini second opinion is now OFF unless `SECOND_OPINION=true` (returns `unclear` "second opinion is off", no network); `no-store`/nosniff/no-referrer/X-Frame-Options/Permissions-Policy/CSP on every response, CORS narrowed to `Content-Type`, per-path body caps (413) and per-IP rate limits (429 + Retry-After) in `backend/security.py`; uvicorn access log drops query strings/client IPs; error logs carry exception types only; upload closed right after reading; 40+ new tests (secrets never echoed by alert/agent errors, SMS body pinned). **Second opinion needs `SECOND_OPINION=true` in `.env` to work.** Spec 00/01/02/05 updated.
- 2026-09-19 — frontend/privacy — **consent before capture + Clear my data**: the home panel now states plainly what happens to video, the speech clip, the voice guide, the alert text and stored data, behind an unchecked consent checkbox; until ticked, Start is disabled, `beginTests()` is a no-op, and the camera (`useMediaPipe`) and mic (`openBrowserMic`) refuse to open. Voice guide is a separate opt-in at Start guide. New `lib/privacy/clearData.ts` (Clear my data button + header logo) stops camera/mic, hangs up the agent and wipes memory, local/sessionStorage and IndexedDB. `frontend/vercel.json` adds CSP + Permissions-Policy (geolocation kept as `self`: the alert needs it). Info FAQ corrected and a 'What data does this use?' entry added. Store gained `consented`/`voiceConsent`/`clearAll` (not a contract change). **Found: the ElevenLabs SDK pulls a libsamplerate worklet from cdn.jsdelivr.net on Safari/Firefox (CSP allows that one file; self-host is a follow-up); second-opinion opt-in text exists but has no UI yet; CSP not verified in a live browser; the Railway CSP wildcard should become the real backend origin.** Demo panel still works without consent (it only injects results).
- 2026-09-19 — vision/backend — **Gemini second opinion implemented** (`models/vision.py`, replacing the Claude stub since only free/public APIs are allowed): one `generateContent` call for all frames, JSON-schema output, per-image JPEG/size checks, everything degrades to `unclear`, key only in a header, nothing sensitive logged. Config: `GEMINI_API_KEY`, `GEMINI_MODEL` (`.env.example`). 19 new offline tests; `conftest` blanks the key. Not run against the real API (no key yet) and the frontend does not capture/send frames or show a consent checkbox yet.
- 2026-09-19 — voice-agent — the live ElevenLabs agent now has a compact, source-checked "Stroke facts" section (CDC / NHS / AHA-Red Cross / ASA warmline) in its prompt, answered only when asked in 1-2 sentences, no medical advice beyond it; text-mode probe: signs, aspirin ("no, unless the dispatcher says"), warmline and recovery questions all answered briefly and on-policy. Same text is in `docs/agent-prompt.md` (re-apply if the agent is recreated).
- 2026-09-19 — frontend/agent — arms distance is now **about three feet** (was six): arms screen title + floor-space note, the runtime arms cue sent to the agent (`useAgent.ts`), `docs/agent-prompt.md`, spec 04, AND the live ElevenLabs agent's saved prompt (that one phrase only, patched via the API with a full backup and a diff check; tools and settings untouched). **Untested at 3 ft: a wingspan at ~1 m may not fit a typical webcam's field of view, so the arms framing gate (both wrists in frame) may never pass; check on a real camera and retune `FRAMING_LIMITS` or the distance.**
- 2026-09-19 — frontend/ui — (1) **2x cursor is back**: a teammate's later commit ("restore voice guide and normal cursor", Fatih) had appended a CSS block forcing the system cursor back on because the big cursors "were too large on desktop"; that override is removed at the project lead's request. (2) scroll hand-off another 30% stiffer: 186 px down / 287 px up (swipe 152 / 220). (3) arms body guide redrawn with realistic proportions (head ~23 units wide vs shoulders ~60, short narrow neck, tapering torso; shifted down so the hint pill clears the head).
- 2026-09-19 — frontend/vision — face instruction card now reads "Make your face serious. Smile when prompted." (aligns with the serious-neutral-face wording used by the face screen, the voice agent and the analyzer hint; supersedes the "relax" wording note below).
- 2026-09-19 — frontend/vision — merge note: the face instruction CARD keeps the requested wording "Relax your face. Smile when prompted." (project lead's call) while majesticcoder14's serious-neutral-face wording remains on the face screen text, the voice agent's briefing and the analyzer retry hint. Worth aligning: the analyzer rejects a resting face that is already smiling.
- 2026-09-19 — frontend/vision — smile check: the instruction card now reads "Relax your face. Smile when prompted.", and when the relax timer ends a small "SMILE!" card appears at the top of the camera box for the smile phase. Arms guide: first picture replaced with the arms-out-to-the-sides figure (caption "Arms out to your sides").
- 2026-09-19 — voice-agent/vision — the resting-face step is now a **serious, neutral face with closed lips (no smile, mouth not stretched)**: agent briefing, on-screen title/caption/intro card, info page and the 'already smiling' retry hint all say so. Verified against the live ElevenLabs agent that a user message interrupts the agent mid-speech, so the smile cue cuts off the earlier line. The live ElevenLabs agent's prompt (step 6) and `start_face_test` description were updated via the API to match `docs/agent-prompt.md` / `docs/agent-tools/start_face_test.json` (nothing else in the agent config changed); a text-mode probe confirmed: serious-face instruction, no smile mention, tool call, then the smile cue interrupts and the agent says "Now, please smile as wide as you can and hold it." If the agent is recreated, re-apply those two texts.
- 2026-09-19 — voice-agent — the agent no longer asks for the big smile up front: the face briefing is now relaxed/resting-face only and `faceCues.ts` cues the smile only when the capture leaves the resting-face phase (once per pass, again after an automatic retry). The idle face title is now "Look at the camera". **The ElevenLabs dashboard system prompt and `start_face_test` description must be updated to match `docs/agent-prompt.md` / `docs/agent-tools/start_face_test.json`.** Not verified in a live conversation.
- 2026-09-19 — frontend/vision — **eyes and face checks no longer start instantly**: a 4 s instruction card (solid blue, dead centre of the camera box, with a "Starting in N…" count) shows first; nothing is measured and the framing-wait timeout has not started until it goes. New `intro` capture phase (`CAPTURE_TIMING.introMs`, `introMs`/`introCaption` on the controller); shown once per check per session (retries skip it; cleared when the session returns to idle). Arms is unchanged (it already has its 3-2-1 cue). 3 new capture tests.
- 2026-09-19 — voice-agent — the agent now goes silent (volume 0, mic muted) for the whole speech recording, driven by the recorder state so it also works when Start recording is pressed before the agent finishes talking or calls the tool; audio is restored when the run ends/cancels, and an unusable recording is announced by the agent if no tool call is waiting (`speechAudioGate.ts`, 6 tests). Not verified with a live conversation.
- 2026-09-19 — frontend/ui — home <-> info scroll hand-off is 30% stiffer: 143 px wheel travel down / 221 px up (was 110 / 170); swipes 117 / 169 px.
- 2026-09-19 — frontend/ui — (1) **mic wave really fixed**: the recorder captures the raw signal (AGC/noise suppression off), so real mics are quiet (RMS ~0.005-0.03) and the linear meter drew a near-flat wave; it now maps dBFS -62..-20 onto the bar (`lib/speech/levelMeter.ts`, unit-tested), for both the recorder level and the consent-time monitor. Verified with a synthetic quiet mic (RMS ~0.008): bars reach ~45% height (was ~5%). (2) **arms guide pictures replaced** with the two supplied figures (`public/images/arms-stand.jpg`, `arms-raise.jpg`; old SVG `ArmPoseFigure` removed). (3) the dot test's backdrop box is now 60% transparent (`bg-black/40`). Note: the new pictures show arms raised overhead, while the protocol (and the on-screen text) asks for arms OUT to the sides.
- 2026-09-19 — frontend/vision — **smile test is more forgiving and prompts are louder**: relax phase 1.5 → 3 s, smile 3 → 5 s, framing-loss grace 1.5 → 3 s, auto-retry pause 2 → 4 s (`CAPTURE_TIMING`, `VISION_RETRY_DELAY_MS`; capture/runner tests now derive from the constants). Camera prompts are big solid bars/pills (amber for "move" hints, blue for actions, calm for relax, `text-2xl`+), and the face screen title follows the phase (Relax your face → Now smile big. Hold it.). Still uncalibrated / not tried on a real face.
- 2026-09-19 — backend/alerts — audit: `ruff.toml` pins lint scope (ruff 0.16 default was flagging 63 style-only items; none were bugs; ruff now runs in CI); `DRY_RUN` fails safe (only explicit false/0/no/off arms real SMS); Twilio/ElevenLabs errors no longer leak raw upstream text, alert send is locked, dry-run log has no PII; +34 error-path tests; leftover "call" copy in `.env.example`/docs fixed. Contracts untouched.
- 2026-09-19 — frontend/ui — UX audit: home copy said the app "calls for help" (SMS only); demo badge "live calls armed" -> "live texts armed"; alert actions phase-guarded and `reset()` keeps permissions/agent connection (store tests); voice-guide pill no longer overlaps the header on phones; skip link visible on focus; countdown makes `<main>` inert; transcript strip no longer scrolls the page; info-page "Start the test" starts a fresh session; demo hotkey ignores Ctrl/Cmd+Shift+D; permission listeners no longer leak; menu jumps honour reduced motion.
- 2026-09-19 — vision/speech/agent — retry audit: leaving a check (skip, emergency, logo, info page) now cancels speech and vision runs (stale speech result could jump the flow), a cancelled run is never re-joined, camera `error` is restarted on the next attempt, cancelled agent tools return neutral text and release the muted mic, agent re-briefs on reconnect, useAgent exhaustive-deps warning fixed.
- 2026-09-19 — voice-agent — made tool completion messages test-neutral and strengthened authoritative phase invalidation so late arms/face/eyes results cannot drive speech-step guidance.
- 2026-09-19 — vision/frontend — **fixed the "frozen after a failed check, even when positioned properly" dead end.** Journey tests (`vision/retryJourney.test.ts`) drive the real runner + retry wrapper + stores: the ChatGPT retry fixes hold for timeouts, both automatic attempts failing, arms, pre-smile, StrictMode double-mount and mid-capture loss, but two dead ends remained and are now fixed: (1) a crash inside a capture (camera/engine exception) left the runner wedged forever (no run, no button) — `start()` now always turns it into a normal retry result and clears its state; (2) a run cancelled by another test (e.g. the voice agent's tool starting a different test) left the screen with no result and no button — `VisionRetryButton` now also appears after ~0.8 s idle with no result (`visionActionState`, pure + tested). Speech was checked: its button is always present. Not verified on a real camera.
- 2026-09-19 — alerts — added `scripts/sms_check.py` (read-only readiness check; `--send` sends one real test SMS through the app's own alert path) and `docs/SMS-SETUP.md`. Ran against the real account: credentials valid (Trial, active) but **the account has no phone number and the demo phone is not verified** — those two steps block SMS; `TWILIO_FROM_NUMBER` is empty.
- 2026-09-19 — voice-agent/ui — restored the visible top-center Start guide control and replaced the oversized custom cursor with standard browser cursors.
- 2026-09-19 — voice-agent/config — synced the latest main branch with Eyes → Face → Arms → Speech, guarded agent tools, and button-only speech recording.
- 2026-09-19 — frontend/ui — info "BE-FAST in four checks" now follows the new test order (Eyes 01, Face 02, Arms 03, Speech 04; Time stays 05; the menu's process list follows). Stat digits roll 0.3 s longer. Scroll-UP back to the check now needs 170 px of wheel travel (130 px swipe) vs 110 px down, so a stray upward scroll is less likely to eject you from the info page.
- 2026-09-19 — frontend/ui — **test order is now Eyes → Face → Arms → Speech** (speech last; `testSequence()`, store + speechRunner tests updated; speech screen tells the patient to move back close). **Waveform fix:** the wave only read the consent-time mic monitor, which has no stream unless "Allow" was pressed this session, so it stayed flat while the recorder heard you; it now also plots the recorder's level (`useSpeechProgress.level`), whichever is louder.
- 2026-09-19 — alerts/contracts — Twilio is now messaging-only: removed the voice-call/TwiML path and `callSid` contract field; countdown and public copy now promise one SMS to the configured demo number.
- 2026-09-19 — alerts — audited the Twilio path and ran a safe dry-run successfully; local `DEMO_PHONE_NUMBER` is present, but the account SID, auth token, and sender number must be configured before a live SMS test.
- 2026-09-19 — vision/frontend — hardened failed-smile recovery: the automatic-retry pause is now explicit state, suppresses the manual button until it is truly needed, and tells a pre-smiling patient to relax before capture restarts.
- 2026-09-19 — voice agent — merged the signed-URL backend, `useAgent` conversation hook, transcripts, and real speech/vision client tools while preserving the production speech recorder; live credentials and end-to-end behavior remain unverified.
- 2026-09-19 — frontend/ui — resolved the duplicate `goHome` declaration introduced when the latest logo-navigation commits met on `main`; the logo keeps the full cancel/reset behavior and the merged frontend compiles again.
- 2026-09-19 — vision/frontend — fixed camera checks becoming idle after bad framing or a pre-smile: face, eyes and arms now retry once automatically, then show a persistent **Try again** action; framing times out at 12 s so recovery begins before Skip appears.
- 2026-09-19 — frontend/ui — mouse cursor is 2x the default size site-wide (`index.css`): SVG arrow, pointing hand over anything clickable, I-beam in text fields; falls back to system cursors if unsupported.
- 2026-09-19 — frontend/ui — home <-> info hand-off now has buffer: the page rubber-bands with your scroll (nudges and dims as it fills), needs 110 px of travel, and the swap is a 0.24 s fade-slide (outgoing page pinned to its route, so no flash of the next page) in the direction of travel (instant under reduced-motion). Menu jumps to a section survive the animated swap (`lib/anchorTarget.ts`).
- 2026-09-19 — frontend/ui — info "numbers" now roll up from zero like a slot machine (`ui/SlotNumber`, starts when scrolled into view, instant under reduced-motion, real value as aria-label). Clicking the logo is a full reset to the home screen (cancels running tests, clears the session). "Who built this" is now Sathvik S., Fatih C., Yeongsu K., Leo Y., Johns Hopkins University (roles removed rather than guessed).
- 2026-09-19 — frontend/ui — logo navigation now returns to the idle homepage from any test or result phase.
- 2026-09-19 — frontend/ui — **header menu is now a drilldown list** (21st.dev "Drilldown Menu" by ruixen.ui, adapted to our tokens and a11y labels; no chevrons on rows): collapsed it shows only "Learn more", opening reveals the sections, and "The process" / "Questions & hotlines" drill one level deeper; leaves scroll to new anchors on the info page (`step-*`, `hotlines`, `faq`). New dep `framer-motion` — **run `pnpm install`**.
- 2026-09-19 — speech — fixed the first real-mic false positive: healthy 2.64 syl/s is no longer "very slow," HNR is report-only for running speech, and the clean run's anonymous metrics are a regression fixture.
- 2026-09-19 — calibration UI — split recorder routes: `?record=speech` shows only speech controls, `?record=vision` only vision, while `?record=1` remains the combined legacy mode.
- 2026-09-19 — calibration — started vision + speech tuning: eyes now save labelled calibration recordings alongside face/arms; claimed real-hardware and real-mic validation work.
- 2026-09-19 — frontend/ui — **merged `main` into `leo/dev`** (speech module + validation tooling landed there). `SpeechTest` now drives `speechRunner.runSpeech()` instead of the old `recordSpeech()` stub (whose return type changed to `SpeechRecording`); `App.tsx` keeps my screen routing and adds the `?record=1` `SpeechRecordPanel`. One-line fix in `speechRunner.test.ts`: it assumed speech is followed by arms, which the new Speech → Eyes → Face → Arms order changes, so it now seeds the eyes result too. 358 frontend tests pass.
- 2026-09-19 — frontend/ui — **typography, copy and behavior pass**: titles are now Tiempos and body Avenir (real fonts first, bundled Newsreader / Nunito Sans as fallbacks; Geist removed; **run `pnpm install`**, new deps `@fontsource-variable/newsreader` + `nunito-sans`, dropped the two Geist ones). Type floor raised from 11 px to 13 px. Info section 02 rewritten in plain conversational copy, no em dashes. Info page now scrolls **up** back to the check (shared `lib/useScrollHandoff.ts`, with a cooldown so inertia can't bounce you back); the Back button stays. Skip offer is a bordered blue card in the bottom-right, no longer covering Start recording. **Tiempos still needs licensed .woff2 files** to appear on other machines.
- 2026-09-19 — frontend/ui — fixed the skip button never appearing (the page was scroll-locked during checks and the button sat below the fold on laptop-height windows; it is now pinned to the bottom of the window on all four checks, and the scroll lock is gone, which also un-stuck the result screen). Fixed the home → info scroll hand-off hanging (its effect re-subscribed every render and wiped the accumulated travel); it now commits at 150 px of wheel travel, and also works by touch swipe and ↓/PageDown/End. Home copy simplified; home now fits 1280×720 without scrolling.
- 2026-09-19 — frontend/ui — **full UI/UX build from the storyboard**: design system (`index.css` tokens, Geist + Geist Mono self-hosted, one blue accent, red reserved for emergency, no gradients, AA contrast, reduced-motion), `ui/` primitives + inline icon set, home with consent panel and scroll-buffer hand-off, `TestScreen` shell (progress dots, transcript strip, mute warning, **15 s skip hatch**), Speech/Eyes/Face/Arms screens with head + body "stand here" guides, info document, 3-band result screen, rebuilt dashboard and countdown. Store gained `route`, `skipped`, `permissions`, `transcript`, `micMuted`; `lib/media/` gained permissions + mic monitor. New deps: `@fontsource-variable/geist`, `@fontsource-variable/geist-mono` — **run `pnpm install`**. 186 frontend tests.
- 2026-09-19 — vision/config — **test order is now Speech → Eyes → Face → Arms** and `FEATURES.eyesTest` is **ON**: `createEyesCapture` (capture.ts) + `runEyes` (useTestRunner.ts) wired through the marked extension points, `EyeStimulus` rendered by `EyeTest`. Arms still last (one step back). Specs 00/02/06 updated. **Nothing verified on a real camera — the eyes left/right mapping is still assumed.**
- 2026-09-19 — validation — added held-out validation tooling for vision (`pnpm tune/freeze/validate`) and speech (`python -m models.validate`), structured recording `conditions` + device `env`, `docs/VALIDATION.md` runbook, `tests/conftest.py` (pins PHONEME_SCORING/DRY_RUN so a local `.env` can't change test results), `PHONEME_THREADS` documented (benchmark: 8 threads 0.45 s vs 4 threads 0.77 s per 5 s clip). Node loader for `pnpm calibrate` now reads files directly (the glob loader ran out of heap at ~280 recordings).
- 2026-09-19 — speech — merged core analysis (DSP + scoring + QC), optional PyTorch phoneme scoring (`mostafaashahin/wav2vec2-base-timit-phoneme-arpa-39`), browser recorder/panels, calibration CLI, hardened endpoint, startup warm-up (`backend/main.py` lifespan). Integration choices: PER scored once (articulation), not reused for intelligibility; phoneme ramps per 0.25→0.60 / gop −1.5→−4.5; `PHONEME_MODEL` id fixed. 165 backend tests; TTS end-to-end run: clean 0.16, slowed 0.94, 10 dB noise 0.32. **Nothing validated on real speech yet.**
- 2026-09-18 — calibration — added `?record=1` recording panel (labelled scenarios, auto-download), `lib/calibration/*` (schema, replay, evaluation against severity anchors), `pnpm calibrate` report, committed-fixture regression test, `docs/CALIBRATION.md`. Personal recordings go in gitignored `frontend/recordings/`.
- 2026-09-18 — vision/frontend — merged the live-camera runtime: `useMediaPipe` (shared engine, GPU→CPU fallback, offline wasm + models), pure `capture.ts` state machine (framing gate → neutral/smile or 3-2-1/10 s hold), `useTestRunner` (`runFace`/`runArms`), `CameraView`, `?debug=1` panel, TEMP manual test buttons in `App.tsx`. **Run `pnpm install` again** (postinstall copies the MediaPipe wasm, ~35 MB, gitignored). 157 frontend tests. Nothing verified on a real camera yet.
- 2026-09-18 — vision — merged face, arms and eyes analysis (`analyzeFace/Arms/Eyes`, `EyeStimulus`, ~113 frontend tests). Shared conventions enforced by `vision/consistency.test.ts` (severity anchors healthy ≤ 0.15 / borderline ≈ 0.35 / clear ≥ 0.85, one retry cutoff `MIN_CONFIDENCE`, patient-side left/right, epoch `startedAt` via `vision/time.ts`, 16:9 default aspect). Spec 02 updated with as-built weights/ramps (arms + eyes weights differ from the original spec to hit the anchors) and naming (`_left/_right`).
- 2026-09-18 — team — added `docs/GETTING-STARTED.md` (step-by-step teammate onboarding); owners are fluid (claim rows in this table).
- 2026-09-18 — tooling — added `docs/STATUS.md`, `scripts/check-docs.sh`, `.githooks/pre-push`, CI workflow. Run `bash scripts/setup-hooks.sh` once per clone.
- 2026-09-18 — vision/frontend — patient positioning: test order is now Face → (Eyes) → Speech → Arms; `framing.ts` gates tell the patient to move closer / step back; store has `hint`; progression = first test without a usable result.
- 2026-09-18 — contracts/config — added `eyes` test type, `FEATURES.eyesTest` (off), `PHONEME_SCORING` (off), specs for both stretches.
- 2026-09-18 — foundation — initial scaffold, alert path with demo-number guard, kickoff prompts.
