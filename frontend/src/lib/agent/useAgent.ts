// OWNER: Frontend/Agent dev. Spec: docs/spec/04-voice-agent.md
// TODO: wrap @elevenlabs/react useConversation({ clientTools }), start with a signed URL from api.signedUrl()
// on a user gesture, mute the mic during the speech test, and mirror connection state into the store.
// Check the current ElevenLabs Agents SDK docs for exact option names.
export {}
